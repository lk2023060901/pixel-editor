# Demo Project

This directory contains an importable example project for the editor.

- `project.json`: example project definition
- `assets/`: example tileset images referenced by the project

The web app resolves this example through its example-project loader and asset API,
instead of embedding demo fixtures inside runtime source modules.

# `@pixel-editor/project`

Responsibility:

- own project-metadata focused command composition
- keep project document mutations out of UI and controller call sites
- expose typed workspace commands that can be reused by persistence flows

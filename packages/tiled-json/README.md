# `@pixel-editor/tiled-json`

Responsibility:

- parse `TMJ/TSJ` JSON documents into typed, normalized adapter results
- keep external format concerns out of UI and editor-state packages
- surface unsupported or lossy input as explicit import issues

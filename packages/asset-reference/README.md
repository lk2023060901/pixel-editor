# `@pixel-editor/asset-reference`

Responsibility:

- normalize project-relative asset paths without depending on Node-specific path APIs
- resolve external format references against a document path and project asset roots
- expose typed reference descriptors for tilesets, images, templates, and other imported assets

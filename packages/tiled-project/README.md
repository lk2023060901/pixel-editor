# `@pixel-editor/tiled-project`

Responsibility:

- parse `.tiled-project` JSON documents into typed project metadata
- export deterministic `.tiled-project` JSON output from the domain project model
- surface unsupported or lossy project metadata as explicit import issues
